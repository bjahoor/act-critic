# Phase 00 — Brainstorm

Formed the idea. ACT with a built-in failure head: one model, one forward pass, outputs an action chunk and a live "am I failing now" score. Most runtime failure detectors run as a second model alongside the policy; this one lives inside it.

No code. Design and scoping only.
